import 'package:flutter/material.dart';

void debugPrintWidgetTree(BuildContext context) {
  // Get the current widget tree
  // final widgetInspectorService = WidgetInspectorService.instance;
  // final currentWidgetTree = widgetInspectorService.getRootWidget('');

  // debugPrint the widget stack to the console
  // debugPrintWidget(currentWidgetTree, 0);
}
