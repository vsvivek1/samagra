import 'package:flutter/material.dart';

class InventoryRequestsTab extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Display requested inventory items here'),
    );
  }
}
